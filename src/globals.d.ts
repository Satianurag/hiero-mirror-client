/**
 * Build-time constant injected by tsdown via the `define` option.
 * Replaced with the literal version string from package.json during bundling.
 *
 * @internal
 */
declare const __HIERO_VERSION__: string;
